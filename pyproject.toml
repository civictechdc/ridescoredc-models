[project]
name = "ridescore"
version = "0.1.0"
description = "RideScore DC — the bike safety scoring pipeline"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }

dependencies = [
    "geopandas>=1.0",
    "shapely>=2.0",
    "pyarrow>=17.0",
    "pandas>=2.2",
    "requests>=2.32",
    "python-dateutil>=2.9",
    "typer>=0.12",
]

[project.scripts]
ridescore = "ridescore.cli:app"

[project.optional-dependencies]
db = ["psycopg[binary]>=3.2", "yoyo-migrations>=9.0"]
# Only the notebook draws anything; the pipeline itself needs no plotting.
notebook = ["matplotlib>=3.8", "jupyterlab>=4.0"]

[dependency-groups]
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.6"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ridescore"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# No test may reach the network. Anything needing a fixture that does not exist
# yet should be marked and skipped, never quietly fetched.
addopts = "--strict-markers"
markers = ["slow: end-to-end over the committed extract"]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

[tool.ruff.lint.per-file-ignores]
# A yoyo migration puts LIVE_DIR on sys.path so it can import the helpers that
# live beside it, which has to happen before those imports run. E402 is inherent
# to the file kind, not a lapse in one file.
"live/migrations/*.py" = ["E402"]
