"""Prototype pieces: a second geography, a loader, descriptions, a manifest.

Everything under `demo/` is deliberately quick. It exists to show that a
parquet file can reach a map without either end knowing about the other, and
it skips the validation, versioning and tests the real steps will need.

Kept in one package so it is obvious what is prototype and what is pipeline.
"""
